const { isMAtch } = require("../10.isMatch");

describe("isMatch()", () => {
  test("isMatch(value) should be result", () => {
    expect(isMAtch(value)).toBe(result);
  });

  test("isMatch(value) should be result", () => {
    expect(isMAtch(value)).toBe(result);
  });
});
