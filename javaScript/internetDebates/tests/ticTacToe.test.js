const { checkBoardstate } = require("../ticTacToe");

describe("checkBoardstate()", () => {
  test("checkBoardstate(value) should be result", () => {
    expect(checkBoardstate(value)).toBe(result);
  });
});
