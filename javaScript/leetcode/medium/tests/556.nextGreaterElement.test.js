const { nextGreaterElement } = require("../556.nextGreaterElement");

describe("nextGreaterElement()", () => {
  test("nextGreaterElement(12) should be 21", () => {
    expect(nextGreaterElement(12)).toBe(21);
  });

  test("nextGreaterElement(21) should be -1", () => {
    expect(nextGreaterElement(21)).toBe(-1);
  });

  test.skip("nextGreaterElement(int) should be result", () => {
    expect(nextGreaterElement(int)).toBe(result);
  });
});
