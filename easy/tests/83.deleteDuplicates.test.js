const { TestScheduler } = require("jest");
const { deleteDuplicates } = require("../83.deleteDuplicates");

describe("deleteDuplicates", () => {
  test("deleteDuplicates(value) should be result", () => {
    expect(deleteDuplicates(value)).toBe(result);
  });
});
