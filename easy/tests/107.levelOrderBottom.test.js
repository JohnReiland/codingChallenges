const { levelOrderBottom, TreeNode } = require("../107.levelOrderBottom");

describe("levelOrderBottom", () => {
  test("levelOrderBottom(value) should be result", () => {
    expect(levelOrderBottom(value)).toBe(result);
  });
});
