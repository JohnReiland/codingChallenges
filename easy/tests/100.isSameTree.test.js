const { isSameTree, TreeNode } = require{ '../100.isSameTree'};

describe('isSameTree', () => {
  test('isSameTree(value) should be result', () => {
    expect(isSameTree(value)).toStrictEqual(result);
  })
})