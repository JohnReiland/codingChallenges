const {
  checkArithmeticSubarrays,
} = require("../1630.checkArithmeticSubarrays");

describe("checkArithmeticSubarrays()", () => {
  test("checkArithmeticSubarrays(value1, value2, value3) should be result", () => {
    expect(checkArithmeticSubarrays(value1, value2, value3)).toStrictEqual(
      result
    );
  });
});
