const { longestSubstring } = require("../395.longestSubstring");

describe("longestSubstring()", () => {
  test.skip(`longestSubstring("value") should be result`, () => {
    expect(longestSubstring("value")).toBe(result);
  });
});
