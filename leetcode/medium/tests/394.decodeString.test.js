const { decodeSTring } = require("../394.decodeString");

describe("decodeString()", () => {
  test("decodeString(value) should be result", () => {
    expect(decodeSTring(value)).toBe(result);
  });
});
